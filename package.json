{
  "name": "daveenci",
  "version": "1.0.0",
  "private": true,
  "description": "DaVeenci - AI & Automation Consultancy with AI-Powered Blog",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:*",
    "dev:web": "npm run dev --workspace=@daveenci/web",
    "dev:api": "npm run dev --workspace=@daveenci/api",
    "build": "npm run build --workspace=@daveenci/web",
    "start:api": "npm run start --workspace=@daveenci/api",
    "lint": "npm run lint --workspace=@daveenci/web",
    "clean": "rm -rf apps/*/node_modules node_modules",
    "install:all": "npm install && npm install --workspace=@daveenci/web && npm install --workspace=@daveenci/api"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}